"use client";
import Image from "next/image";
import Link from "next/link";
import NavBar from "@/components/NavBar";
import { useAuth } from "@/context/AuthContext";

export default function Home() {
  const { user, loading } = useAuth();
  return (
    <div className="min-h-screen flex flex-col bg-zinc-50">
      <NavBar />
      <main className="flex-1">
        <section className="mx-auto max-w-6xl px-4 py-16 grid md:grid-cols-2 gap-10 items-center">
          <div>
            <h1 className="text-4xl font-semibold tracking-tight">Contenta — AI content assistant for creators</h1>
            <p className="mt-4 text-gray-600">Generate captions, scripts, ideas, and rewrite text with Gemini. Image-based suggestions coming soon.</p>
            <div className="mt-6 flex gap-3">
              {loading ? (
                <span className="text-sm text-gray-500">Checking session…</span>
              ) : user ? (
                <Link href="/dashboard" className="px-4 py-2 rounded-md bg-black text-white">Go to dashboard</Link>
              ) : (
                <>
                  <Link href="/signup" className="px-4 py-2 rounded-md bg-black text-white">Get started</Link>
                  <Link href="/signin" className="px-4 py-2 rounded-md border">Sign in</Link>
                </>
              )}
            </div>
          </div>
          <Image src="/next.svg" alt="logo" width={320} height={80} className="dark:invert justify-self-center" />
        </section>
      </main>
    </div>
  );
}
